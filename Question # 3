#include <iostream>
using namespace std;
int main() {
    int num;
    cout << "Enter an integer value: ";
    cin >> num;

    if (num > 10 && num <= 20) {
        cout << "1" << endl;
    } else {
        cout << "0" << endl;
    }
    return 0;
}
